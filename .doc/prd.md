“拾光机” App（英文名：Time Echo）（离线初期版本）产品与设计大纲
一、项目概述（更新名称寓意，其余保持原内容）
（一）项目背景
（原内容不变，更新名称关联说明）“拾光机”（Time Echo）聚焦 “离线怀旧” 核心场景，“拾光” 直指 “拾起过往时光记忆” 的产品核心，简洁有力且充满情感温度，避免重复表述；英文名 “Time Echo” 意为 “时光回响”，与中文名形成 “行动（拾光）+ 效果（时光回响）” 的呼应，区别于普通 “年龄测试” 类 App，传递 “用题目唤醒时光记忆” 的品牌定位，强化用户对 “怀旧 + 互动” 的认知。
（二）目标用户（原内容不变）
（三）核心功能与独特价值（原内容不变，将 “时光拾光机” 统一替换为 “拾光机”）
二、功能设计（全离线模式）- 名称同步更新，模块逻辑不变
（一）核心模块：怀旧问答模块（“题库本地更新” 子模块名称优化）
问题库设计（本地存储）
（原内容：内容分类、难度分级）
题库本地更新机制（名称不变，内容更新）
更新触发：当用户下载 “拾光机” 新版本（包含题库更新）并安装后，首次启动 App 时，弹出 “题库更新提示”（“发现 10 道新怀旧题目，是否更新本地题库？更新不消耗流量，仅占用 5MB 存储空间”），提供 “立即更新”“稍后更新” 选项。
更新逻辑：选择 “立即更新” 后，通过本地资源替换（新版本内置更新后的 SQLite 数据库）完成题库补充，旧题目保留，新增题目标注 “新题” 标签（持续 7 天，便于用户识别）；选择 “稍后更新”，后续每次启动 “拾光机” 时在首页顶部显示 “待更新：10 道新题” 提示，点击可触发更新。
版本兼容：若用户未升级 App，不强制更新，仅在 “帮助中心 - 常见问题” 说明 “升级‘拾光机’可获取更多怀旧题目”，避免离线场景下的更新压力。
答题流程设计（原内容不变，“时光拾光机” 替换为 “拾光机”）
年龄计算逻辑（原内容不变，“时光年龄” 统一调整为 “拾光年龄”，如 “你的拾光年龄：XX 岁”，强化名称关联）
（二）核心模块：个人中心（离线数据管理）- 模块名称与描述同步更新
测试记录管理（原内容不变，“时光拾光机” 替换为 “拾光机”）
怀旧收藏夹（更名 “拾光收藏夹”，内容优化）
收藏触发：答题时，题目右上角设置 “收藏” 按钮（空心星星图标，优化为 “复古胶片星星” 样式，贴合 “拾光” 主题，星星内部融入 “小相机” 元素），用户点击后变为实心星星，同时弹出 “已收藏至拾光收藏夹” 提示；查看测试记录详情时，每题同样支持 “收藏 / 取消收藏” 操作。
收藏管理：个人中心新增 “拾光收藏夹” 入口（配 “复古胶片星星” 图标），按 “收藏时间” 倒序展示收藏题目，每题显示 “题目分类（影视 / 音乐 / 事件）”“难度”“收藏时间”，点击题目可直接进入 “题目详情页”（显示完整题目、选项、正确答案、解析，无答题交互，仅用于回顾，详情页背景调整为 “旧相纸纹理”，更贴合 “拾光” 记忆感）。
收藏操作：支持 “取消收藏”（长按题目弹出 “取消收藏” 选项）、“批量管理”（勾选多个收藏题目，可 “批量取消收藏”“批量分享题目截图”—— 离线截图分享至本地社交软件，如微信好友、QQ，截图右上角添加 “拾光机” 小 logo，logo 设计为 “相机 + 时光齿轮” 组合，提升品牌辨识度），无收藏时显示 “还未收藏题目，快去答题收藏你的专属时光记忆吧～” 引导文案。
答题成就体系（更名 “拾光成就体系”，成就名称优化）
成就类型（全离线触发，共 8 项基础成就，名称融入 “拾光” 元素）：
成就名称
达成条件
成就奖励（离线专属，奖励描述关联名称）
拾光初遇
完成首次测试
解锁 “拾光徽章・初遇”（个人中心头像旁显示，徽章设计为 “复古相机” 样式）
影视拾光者
影视分类题库正确率≥90%（单次测试）
解锁 “影视徽章・拾光”+ 收藏夹容量 + 5 题 +“影视拾光语录”（结果页随机显示，如 “每帧画面，都是时光的印记～”）
音乐回响者
音乐分类题库正确率≥90%（单次测试）
解锁 “音乐徽章・回响”+ 收藏夹容量 + 5 题 +“音乐拾光语录”（结果页随机显示，如 “旋律响起，时光重回眼前～”）
时代见证者
事件分类题库正确率≥90%（单次测试）
解锁 “事件徽章・见证”+ 收藏夹容量 + 5 题 +“事件拾光语录”（结果页随机显示，如 “每段故事，都是时光的礼物～”）
拾光速答手
单次测试单题平均耗时≤15 秒
解锁 “速答徽章・拾光”+ 拾光年龄计算时额外 - 1 岁
拾光挑战者
单次测试困难题正确率 100%
解锁 “挑战徽章・拾光”+ 拾光年龄计算时额外 - 2 岁
拾光收藏家
收藏题目数量≥20 道
解锁 “收藏徽章・拾光”+ 收藏夹容量 + 10 题 +“收藏拾光语录”（收藏夹页面显示，如 “每道收藏，都是时光的碎片～”）
拾光全勤人
连续 7 天每天完成 1 次测试
解锁 “全勤徽章・拾光”+ 随机 “拾光怀旧语录”（结果页显示，如 “7 天拾光之旅，满是回忆的温度～”）

成就展示：个人中心新增 “我的拾光成就” 入口（配 “复古奖杯” 图标，奖杯上添加 “相机” 元素），按 “达成时间” 倒序展示已解锁成就（带彩色 “拾光徽章” 图标 + 达成时间，徽章背景添加 “相纸纹理”），未解锁成就显示灰色图标 + 达成条件；头像旁显示 “已解锁拾光徽章 XX/XX 个”（如 “3/8”），增强用户 “时光拾集者” 的代入感。
成就规则：成就数据本地存储，卸载 “拾光机” 后丢失（个人中心 “我的拾光成就” 页面提示 “拾光成就数据仅保存在本地，卸载 App 将清空，珍惜每一份时光记忆～”）；后续版本更新可新增成就，通过 “题库本地更新” 同步至用户端，已解锁成就不受影响。
个性化设置（原内容不变，“时光拾光机” 替换为 “拾光机”）
个人信息（原内容不变，“时光拾光机” 替换为 “拾光机”）
（三）辅助模块：离线引导与帮助（原内容不变，“时光拾光机” 替换为 “拾光机”）
（四）老年用户专属优化模块（内容更新，名称关联）
交互优化
触控适配：所有按钮（如答题选项、收藏按钮、成就入口）尺寸放大 20%，间距增加 10px，避免老年用户 “误触”；答题时支持 “双击题目文字” 放大（放大至原尺寸 1.5 倍，再次双击恢复），解决 “看不清题目” 问题，按钮图标统一为 “圆润复古风格”，图标边缘添加 “细边框”，提升老年用户视觉辨识度。
语音辅助：在 “个性化设置 - 显示设置” 中新增 “拾光语音读题” 开关（默认关闭，名称添加 “拾光”，强化品牌关联），开启后答题时自动读取题目内容 + 选项（本地 TTS 语音，语音风格优化为 “温和男声 / 亲切女声”，语速调节新增 “极慢” 选项，适配听力较弱的老年用户），音乐题音频播放时自动暂停语音，播放结束后继续读题，满足 “视力不佳” 老年用户需求。
内容优化
题目文字采用 “无衬线字体”（iOS “苹方 - 简”、安卓 “思源黑体”、鸿蒙 “鸿蒙黑体”），避免 “笔画复杂导致的阅读困难”；选项文字颜色对比度提升至 4.5:1（符合 WCAG accessibility 标准），错误答案 “浅红” 底色调整为 “深灰红”（#C74B50），正确答案 “浅绿” 底色调整为 “深灰绿”（#2E7D32），增强视觉区分度，颜色选择更贴近 “复古怀旧” 色调（如避免高饱和度色彩）。
结果页 “趣味评语” 增加 “老年友好版”（如原评语 “你是复古圈的新势力” 调整为 “您对老时光的记忆真清晰，拾光机为您点赞～”，融入产品名称，增强亲切感），在 “个性化设置” 中新增 “拾光评语风格” 选项（默认 “通用版”，可选 “老年友好版”），贴合老年用户语言习惯。
三、界面设计（适配 iOS / 安卓 / 鸿蒙，离线视觉风格）- 名称与视觉元素同步更新
（一）整体设计风格（更新名称视觉化关联）
原内容不变，新增 “名称视觉化关联” 说明：界面装饰元素统一融入 “拾光” 符号，核心符号为 “复古相机、旧相纸、胶片、时光齿轮”，如边框用 “相机镜头刻度纹理”、图标用 “相机 + 胶片” 组合、分割线用 “胶片齿孔样式”；主色调 “焦糖棕” 补充 “拾光棕” 别称，辅助色新增 “浅相纸白”（#F8F5F0），强化 “拾光机”“拾起时光影像” 的品牌视觉识别，让用户从视觉上直观感知 “怀旧 + 记忆” 的产品属性。
（二）核心页面设计（离线适配）- 页面名称与视觉优化
首页（优化名称与视觉）
顶部新增 “题库更新提示条”（仅当有未更新题库时显示，背景色 “浅拾光棕”，文字 “待更新：10 道新拾光题目”+ 右侧 “立即更新” 按钮，按钮图标优化为 “复古相机快门” 样式），点击按钮触发更新；无更新时不显示，保持界面简洁。
首页 Logo 设计：“拾光机” 艺术字采用 “复古手写体 + 粗体” 组合，字体笔画融入 “胶片纹理”，英文 “Time Echo” 置于下方，字体为 “优雅衬线体”，Logo 右侧添加 “迷你复古相机” 图标（相机镜头可轻微旋转，增强动态感），提升品牌记忆点。
答题页（视觉优化）
题目右上角新增 “收藏” 按钮（空心 / 实心 “复古胶片星星” 图标，尺寸 24px，距题目右侧边缘 15px），点击时有 “缩放 + 轻微旋转” 动效（模拟 “按下相机快门” 的感觉，增强交互反馈）；开启 “拾光语音读题” 后，题目左侧显示 “复古麦克风” 图标（播放中为动态旋转，暂停为静态，麦克风顶部添加 “小相机” 元素），点击可暂停 / 继续读题。
拾光收藏夹页面（原 “时光收藏夹”，视觉更新）
顶部区域：标题 “拾光收藏夹”（居中，字体加粗，标题左侧添加 “旧相册” 图标，相册封面融入 “相机图案”），右侧 “批量管理” 按钮（文字按钮，点击进入批量选择模式，显示 “取消”“取消收藏”“分享拾光截图” 按钮，“分享拾光截图” 文字强化名称关联）。
内容区域：卡片式展示收藏题目，每张卡片背景更新为 “旧相纸纹理”，边缘添加 “细黑边框”，模拟 “实体照片” 质感；包含 “题目分类标签（如‘影视・简单’，标签样式优化为‘复古相机拍立得’）”“题目内容（1 行省略，hover 显示完整内容，hover 时卡片轻微抬起，模拟‘拿起照片’效果）”“收藏时间（右下角，如‘2025-10-20’，时间字体为‘复古打字机字体’，颜色为‘深拾光棕’）”“收藏图标（右上角实心‘胶片星星’）”。
底部区域：无收藏时显示 “引导文案（‘还未收藏拾光题目，快去答题收藏你的专属回忆吧～’）+ 复古空相册图标（相册旁添加‘相机’元素）”，有收藏时显示 “共 XX 道拾光收藏题目”（居中，文字旁添加 “迷你胶片” 图标）。
我的拾光成就页面（原 “我的时光成就”，视觉更新）
顶部区域：标题 “我的拾光成就”（居中，字体加粗，标题右侧添加 “复古奖杯” 图标，奖杯底座刻有 “拾光” 二字），右侧 “成就规则” 入口（“拾光问号” 图标，图标为 “问号 + 复古相机” 组合，点击弹出 “拾光成就说明” 弹窗，弹窗背景为 “旧相纸纹理”，包含所有成就达成条件 + 奖励，支持滑动查看）。
内容区域：网格布局（2 列）展示成就，已解锁成就卡片背景色 “浅拾光棕”，带彩色 “拾光徽章” 图标（徽章内融入 “相机 / 胶片 / 日历” 元素，如 “拾光初遇” 徽章为 “相机 + 星星” 组合）+ 成就名称 + 达成时间（时间字体为 “复古手写体”，颜色为 “深拾光棕”）；未解锁成就卡片背景色 “浅相纸白”，带灰色 “拾光徽章” 图标 + 成就名称 + 达成条件（如 “影视拾光者：影视分类题库正确率≥90%”）。
底部区域：显示 “已解锁 XX/XX 个拾光成就”（居中，字体加粗，文字旁添加 “迷你相机快门” 图标），后续版本更新后新增成就将自动添加至最后，标注 “新拾光成就” 标签（标签样式为 “复古红戳”，戳面印有 “新” 字 + 相机图案，持续 7 天）。
老年友好版界面适配（视觉优化）
开启 “拾光语音读题” 后，答题页顶部显示 “拾光语音读题中” 提示条（背景色 “浅相纸白”，文字 “语速：中”，点击可切换语速，切换按钮为 “圆润复古样式”，按钮表面添加 “细纹理”，提升触控辨识度）；“老年友好版” 评语在结果页显示时，字体自动放大至 “大号”，评语旁添加 “迷你老照片” 图标（照片内容为 “复古场景”，如老街道、旧电视），增强亲切感。
（三）跨平台适配要点（原内容不变，“时光拾光机” 替换为 “拾光机”）
四、技术实现（全离线架构）- 名称与字段同步更新
（一）跨平台开发框架选择（原内容不变，“时光拾光机” 替换为 “拾光机”）
（二）离线数据存储方案（字段名称优化，关联 “拾光”）
问题库存储（字段更新）
新增 “question_update_log” 表（SQLite），字段 “app_name” 值更新为 “拾光机”，确保与产品名称一致；新增题目在 “question” 表中的 “time_theme” 字段优化为 “echo_theme”（呼应英文名 “Time Echo”，字段值如 “80 年代影视”“90 年代音乐”），便于后续主题筛选与名称关联。
用户数据存储（表名与字段优化）
拾光收藏夹数据：原 “time_collection” 表更名 “echo_collection”（呼应英文名），字段 “collection_note” 优化为 “echo_note”（预留字段，后续版本支持 “添加拾光笔记”，如 “这道题让我想起小时候看的电影～”），通过 “question_id” 关联获取题目详情，支持按 “collection_time” 排序查询。
拾光成就数据：原 “time_achievement” 表更名 “echo_achievement”，字段 “achievement_name”“achievement_icon”“reward” 内容同步更新为 “拾光” 相关（如 “achievement_name” 值为 “拾光初遇”，“achievement_icon” 路径为 “icons/echo_badge_first.png”，“reward” 值为 “解锁拾光徽章・初遇”）；启动 “拾光机” 时自动查询该表，展示已解锁成就；答题结束后触发 “拾光成就判定逻辑”（本地 Dart 代码，函数名更新为 “echo_check_achievement ()”），满足条件则更新 “is
